#!/usr/bin/python3
def number_keys(a_dictionary):
    """number_keys function

    Args:
        a_dictionnary: input dictionary.

    Returns:
        number of keys in the dictionary.
    """
    return len(a_dictionary.keys())
